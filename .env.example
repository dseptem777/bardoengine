# BardoEngine encryption key — exactly 32 ASCII characters
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
BARDO_ENCRYPTION_KEY=your_32_byte_key_here_00000000000
